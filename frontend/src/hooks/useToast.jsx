// Re-exports from the canonical toast.jsx implementation.
// Both ToastProvider (used in main.jsx) and useToast (used in pages) come from here.
export { ToastProvider, useToast } from "@/components/ui/toast";
